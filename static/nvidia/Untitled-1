so I think I may need to have a private constructor and will use a static method to generate the first new instance, I need to have inside of the class (in the object) a list that would be containing generic functions... those function in real life have an already well defined shape but to keep it simple for you I will use this simple definition I want to add functions to a list one step at the time the function would be of type `(val: InputType) => OutputType`  but this is the generic basic type in reality I want to provide the function one at the time the first one would be in the static factory method the consecutive iterations (dased on the chaning I mentioned earlier